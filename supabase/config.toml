# Supabase CLI configuration.
#
# The Atlas concierge must be callable by ANONYMOUS visitors (it walks beside
# signed-out travelers too), and this project uses the new sb_publishable_ key,
# which is NOT a JWT. So the atlas Edge Function must not enforce JWT
# verification — otherwise the gateway rejects every call with a 401 before the
# function runs. (Deploy via CLI honors this; the Dashboard has an equivalent
# per-function "Verify JWT" toggle.)
project_id = "xgjidkgctqqdprxtxeui"

[functions.atlas]
verify_jwt = false

# Same reasoning — the image proxy is called from anonymous browsing too.
[functions.unsplash]
verify_jwt = false

# Live voice must work for an anonymous traveler (no account needed to start —
# and the VC demo is anonymous), so the token minter can't demand a JWT. Safe:
# it grants only roomJoin on one room for 15 minutes, and the LiveKit API secret
# never leaves the server. Same posture as `atlas`.
[functions.livekit-token]
verify_jwt = false
